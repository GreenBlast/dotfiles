 __     __ _                       
 \ \   / /(_) _ __ ___   _ __  ___ 
  \ \ / / | || '_ ` _ \ | '__|/ __|
   \ V /  | || | | | | || |  | (__ 
    \_/   |_||_| |_| |_||_|   \___|
                                   
yadm check the ignore instead of untracked and set the untracked in the bootstrap
-- yadm gitconfig --unset status.showUntrackedFiles

add folders to the ignore exclude

Add to included profile script if not exist (See FZF)
make term var in tmux tmux-256color - and or also in bashrc, while adding bashrc to dotfiles
if [ 'find /lib/terminfo /usr/share/terminfo -name "*256*" | grep xterm-256color' ]; then
    export TERM='xterm-256color'
else
    export TERM='xterm-color'
fi


Check if should and use crypt


" markdown
Plug 'itspriddle/vim-marked', { 'for': 'markdown', 'on': 'MarkedOpen' } " Open markdown files in Marked.app - mapped to <leader>m
Plug 'tpope/vim-markdown', { 'for': 'markdown' } " markdown support

" Async make
Plug 'Shougo/vimproc.vim', { 'do': 'make' } " interactive command execution in vim
 Choose Files Choose Files


Plug 'tpope/vim-ragtag' " endings for html, xml, etc. - ehances surround
garbas/vim-snipmate
" Improves the builtin netrw
tpope/vim-vinegar

" Session management needs to compare with the current
tpope/vim-obsession

Also check tmux resurrect for vim sessions:
# for vim
set -g @resurrect-strategy-vim 'session'
# for neovim
set -g @resurrect-strategy-nvim 'session'


" buffer keys
nnoremap <Leader>bb :b#<CR>
nnoremap <Leader>bn :bn<CR>
nnoremap <Leader>bp :bp<CR>
nnoremap <Leader>bf :bf<CR>
nnoremap <Leader>bl :bl<CR>
nnoremap <Leader>bw :w<CR>:bd<CR>
nnoremap <Leader>bd :bd!<CR>
" new buffer/tab
nnoremap <Leader>e :enew<CR>

" window keys
nnoremap <Leader>w< <C-w><
nnoremap <Leader>w> <C-w>>
nnoremap <Leader>w- <C-w>-
nnoremap <Leader>w+ <C-w>+
nnoremap <Leader>ws :split<CR>
nnoremap <Leader>wv :vsplit<CR>
nnoremap <Leader>wx :close<CR>

" %% to expand active buffer location on cmdline
cnoremap <expr> %% getcmdtype() == ':' ? expand('%:h').'/' : '%%'

" CtrlP keys
nnoremap <Leader>pp :CtrlP<CR>
nnoremap <Leader>pf :CtrlP<CR>
nnoremap <Leader>pm :CtrlPMRUFiles<CR>
nnoremap <Leader>pr :CtrlPMRUFiles<CR>
nnoremap <Leader>pb :CtrlPBuffer<CR>

---

Add hebrew?
